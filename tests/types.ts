export interface Character {
  name: string;
  role: string;
  birth: number;
}

export interface SimpleCharacterKey {
  name: string;
}

export interface ComplexCharacterKey {
  name: string;
  birth: number;
}
